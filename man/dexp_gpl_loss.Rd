% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpl_loss.R
\name{dexp_gpl_loss}
\alias{dexp_gpl_loss}
\title{Derivative of the expected gpl loss}
\usage{
dexp_gpl_loss(g = "x", dg = NULL, F, kappa = 1, alpha = NA, O = NA, U = NA)
}
\arguments{
\item{g}{a non-decreasing increment function, supplied either as a function
or as a string in the variable \code{x} such as \code{"log(x)"}.}

\item{dg}{derivative of \code{g}; if \code{NULL} or \code{NA} it is derived from \code{g}.}

\item{F}{predictive cdf of the outcome.}

\item{kappa}{scale factor.}

\item{alpha}{normalized loss when the outcome \code{y} exceeds the allocation \code{x}.
Exactly one of \code{alpha} and \code{U} must be supplied.}

\item{O}{cost incurred when the allocation \code{x} exceeds the outcome \code{y};
equals \code{kappa * (1 - alpha)}.}

\item{U}{cost incurred when the outcome \code{y} exceeds the allocation \code{x};
equals \code{kappa * alpha}.}
}
\value{
A function of an allocation \code{x} giving the derivative of the
expected loss with respect to the distribution \code{F}.
}
\description{
For the linear increment function this is
\deqn{\kappa\, g'(x)\, (F(x) - \alpha),}
the quantity whose root gives the unconstrained optimal allocation.
}
\examples{
# zero at the median of the predictive distribution
dexp_gpl_loss(F = pnorm, alpha = 0.5)(0)

}
