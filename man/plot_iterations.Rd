% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_iterations}
\alias{plot_iterations}
\title{Plot the allocation search for one budget}
\usage{
plot_iterations(
  adf,
  K_to_plot = NULL,
  itnum = NULL,
  num_targets_to_color = 6,
  target_palette = (scales::viridis_pal())(num_targets_to_color + 1)
)
}
\arguments{
\item{adf}{an \code{allocated} object; see \code{\link[=allocate]{allocate()}}.}

\item{K_to_plot}{the budget whose search to show. Required unless \code{adf} has
a single row.}

\item{itnum}{how many iterations to show. Defaults to all of them.}

\item{num_targets_to_color}{how many of the largest targets to colour
individually; the rest are pooled into "other".}

\item{target_palette}{colours for the individually coloured targets.}
}
\value{
A \link[patchwork:patchwork]{patchwork::patchwork} object stacking the two panels.
}
\description{
Shows how the allocation evolved over the bisection on lambda, as a stacked
area of allocations above a panel tracking lambda and its bracketing
interval.
}
\examples{
fc <- add_pdqr_funs(
  tibble::tibble(
    target_names = c("a", "b", "c"), dist = "norm",
    mean = c(5, 8, 12), sd = c(1, 2, 3)
  ),
  types = c("p", "q")
)
plot_iterations(allocate(fc, K = 20, alpha = 0.9))

}
