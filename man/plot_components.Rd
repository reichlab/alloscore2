% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_components}
\alias{plot_components}
\alias{plot_components.default}
\alias{plot_components.allocated}
\title{Plot the per-target components of an allocation score}
\usage{
plot_components(...)

\method{plot_components}{default}(
  df,
  Ks = NULL,
  scored_col_name = "scored",
  origin_time_col_name = NULL,
  model_col_name = NULL,
  target_col_name = NULL,
  show_oracle = TRUE,
  ...
)

\method{plot_components}{allocated}(
  ...,
  Ks = NULL,
  origin_time_col_name = NULL,
  model_col_name = NULL,
  target_col_name = NULL,
  show_oracle = TRUE
)
}
\arguments{
\item{...}{objects to plot. For the default method, a data frame with a list
column of scored allocations; for the \code{allocated} method, one or more
scored \code{allocated} objects.}

\item{df}{a data frame with a list column of scored allocations.}

\item{Ks}{budgets to plot. Defaults to \code{NULL}, which keeps all of them; note
that the bar layout requires a single budget.}

\item{scored_col_name}{name of the list column holding the scored
allocations.}

\item{origin_time_col_name}{name of the column holding an origin time, used
to facet.}

\item{model_col_name}{name of the column holding a model name. For hubverse
model output this is \code{"model_id"}.}

\item{target_col_name}{name of the column holding target names.}

\item{show_oracle}{whether to add the oracle's components as an extra model.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
\description{
Shows how a score decomposes across targets: as stacked or dodged bars when
a single budget is plotted, and as stacked areas over the budget otherwise.
}
\section{Methods (by class)}{
\itemize{
\item \code{plot_components(default)}: Plots a data frame holding a list column of
scored allocations, such as one row per model and origin time.

\item \code{plot_components(allocated)}: Plots one or more scored \code{allocated} objects.

}}
\examples{
fc <- add_pdqr_funs(
  tibble::tibble(
    target_names = c("a", "b", "c"), dist = "norm",
    mean = c(5, 8, 12), sd = c(1, 2, 3)
  ),
  types = c("p", "q")
)
scored <- alloscore(fc, y = c(4, 9, 11), K = c(10, 20, 30))
plot_components(scored)

}
