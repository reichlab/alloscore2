% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_scores_slim}
\alias{plot_scores_slim}
\title{Plot allocation scores against the budget or over time}
\usage{
plot_scores_slim(
  slim_df,
  Ks = NULL,
  ts = FALSE,
  ts_dates = NULL,
  origin_time_col_name = NULL,
  model_col_name = NULL,
  target_col_name = NULL,
  show_oracle = FALSE,
  show_raw = FALSE,
  order_at_K = NULL,
  order_at_model = NULL,
  order_at_origin_time = NULL,
  palette = NULL,
  linetypes = NULL,
  ytot = TRUE
)
}
\arguments{
\item{slim_df}{a slim scored allocation; see \code{\link[=slim]{slim()}}.}

\item{Ks}{budgets to plot. Defaults to \code{NULL}, which keeps all of them; note
that the bar layout requires a single budget.}

\item{ts}{plot the score as a time series over \code{origin_time} rather than
against the budget. Requires one budget per origin time, given in \code{Ks}.}

\item{ts_dates}{restrict a time series plot to these origin times.}

\item{origin_time_col_name}{name of the column holding an origin time, used
to facet.}

\item{model_col_name}{name of the column holding a model name. For hubverse
model output this is \code{"model_id"}.}

\item{target_col_name}{name of the column holding target names.}

\item{show_oracle}{whether to add the oracle's components as an extra model.}

\item{show_raw}{plot the forecaster's own losses rather than its losses
relative to the oracle.}

\item{order_at_K, order_at_model, order_at_origin_time}{order the targets by
their contribution at this budget, model or origin time. By default the
targets are ordered over the whole data set.}

\item{palette}{colours for the models, passed to
\code{\link[ggplot2:scale_color_manual]{ggplot2::scale_color_manual()}}.}

\item{linetypes}{line types for the models, passed to
\code{\link[ggplot2:scale_linetype_manual]{ggplot2::scale_linetype_manual()}}.}

\item{ytot}{add a dashed vertical line at the total weighted outcome, the
budget beyond which the oracle incurs no loss.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
\description{
Plot allocation scores against the budget or over time
}
\examples{
fc <- add_pdqr_funs(
  tibble::tibble(
    target_names = c("a", "b", "c"), dist = "norm",
    mean = c(5, 8, 12), sd = c(1, 2, 3)
  ),
  types = c("p", "q")
)
scored <- alloscore(fc, y = c(4, 9, 11), K = seq(5, 40, by = 5))
plot_scores_slim(slim(scored))

}
