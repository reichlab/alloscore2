% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_out.R
\name{allocate_model_out}
\alias{allocate_model_out}
\title{Allocate a budget across hubverse model output}
\usage{
allocate_model_out(
  model_out_tbl,
  K,
  target_cols,
  w = 1,
  kappa = 1,
  alpha = 1,
  g = "x",
  ...
)
}
\arguments{
\item{model_out_tbl}{a hubverse model output table containing a single
\code{output_type}, which must be \code{"quantile"}.}

\item{K}{vector of budgets. Cannot be supplied via \code{df}.}

\item{target_cols}{character vector naming the task ID columns whose
combinations enumerate the targets that share a budget, for example
\code{"location"}.}

\item{w}{allocation weights: a scalar, a vector named by target, a vector
ordered as the targets of each allocation problem, or the name of a column
of \code{model_out_tbl} holding a per-target weight.}

\item{kappa}{scale factor.}

\item{alpha}{normalized loss when the outcome \code{y} exceeds the allocation \code{x}.
Exactly one of \code{alpha} and \code{U} must be supplied.}

\item{g}{a non-decreasing increment function, supplied either as a function
or as a string in the variable \code{x} such as \code{"log(x)"}.}

\item{...}{further arguments passed to \code{\link[=allocate]{allocate()}}.}
}
\value{
A tibble with one row per allocation unit and value of \code{K}, holding
the allocation unit columns and the columns returned by \code{\link[=allocate]{allocate()}}. Unlike
\code{\link[=allocate]{allocate()}} this is a plain tibble rather than an \code{allocated} object, since
each unit has its own loss functions and weights.
}
\description{
Runs \code{\link[=allocate]{allocate()}} once per allocation unit of a hubverse model output table.
See \code{\link[=as_alloscore_df]{as_alloscore_df()}} for how allocation units are determined.
}
\examples{
\dontshow{if (requireNamespace("hubExamples", quietly = TRUE)) withAutoprint(\{ # examplesIf}
mot <- dplyr::filter(
  hubExamples::forecast_outputs,
  output_type == "quantile", reference_date == "2022-11-19", horizon == 0
)
allocate_model_out(mot, K = c(100, 500), target_cols = "location")
\dontshow{\}) # examplesIf}
}
