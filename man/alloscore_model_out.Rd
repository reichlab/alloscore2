% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_out.R
\name{alloscore_model_out}
\alias{alloscore_model_out}
\title{Score hubverse model output with an allocation scoring rule}
\usage{
alloscore_model_out(
  model_out_tbl,
  oracle_output,
  K,
  target_cols,
  w = 1,
  kappa = 1,
  alpha = 1,
  g = "x",
  against_oracle = TRUE,
  summarize = TRUE,
  by = c("model_id", "K"),
  ...
)
}
\arguments{
\item{model_out_tbl}{a hubverse model output table containing a single
\code{output_type}, which must be \code{"quantile"}.}

\item{oracle_output}{hubverse oracle output, holding the observed values in
an \code{oracle_value} column. Optional when only allocating.}

\item{K}{vector of budgets. Cannot be supplied via \code{df}.}

\item{target_cols}{character vector naming the task ID columns whose
combinations enumerate the targets that share a budget, for example
\code{"location"}.}

\item{w}{allocation weights: a scalar, a vector named by target, a vector
ordered as the targets of each allocation problem, or the name of a column
of \code{model_out_tbl} holding a per-target weight.}

\item{kappa}{scale factor.}

\item{alpha}{normalized loss when the outcome \code{y} exceeds the allocation \code{x}.
Exactly one of \code{alpha} and \code{U} must be supplied.}

\item{g}{a non-decreasing increment function, supplied either as a function
or as a string in the variable \code{x} such as \code{"log(x)"}.}

\item{against_oracle}{logical; if \code{TRUE}, scores relative to the oracle
allocation are included.}

\item{summarize}{logical; if \code{TRUE}, average the scores over the columns
named in \code{by}.}

\item{by}{character vector naming the columns to summarize by. Must be a
subset of the allocation unit columns and \code{"K"}.}

\item{...}{further arguments passed to \code{\link[=allocate]{allocate()}}.}
}
\value{
A tibble of scores. Unsummarized, one row per allocation unit and
value of \code{K}, with columns \code{K}, \code{score}, \code{score_raw}, \code{score_oracle},
\code{ytot} and a nested \code{xdf} of per-target detail. Summarized, one row per
combination of \code{by} with the mean of each score.
}
\description{
For each allocation unit of a hubverse model output table, allocates a budget
under the model's forecasts and scores the realized loss of that allocation
relative to the loss of an oracle that knew the observed outcomes. See
\code{\link[=as_alloscore_df]{as_alloscore_df()}} for how allocation units are determined, and
\code{\link[=alloscore]{alloscore()}} for the score itself.
}
\examples{
\dontshow{if (requireNamespace("hubExamples", quietly = TRUE)) withAutoprint(\{ # examplesIf}
mot <- dplyr::filter(hubExamples::forecast_outputs, output_type == "quantile")
scores <- alloscore_model_out(
  model_out_tbl = mot,
  oracle_output = hubExamples::forecast_oracle_output,
  K = c(500, 1000),
  target_cols = "location",
  by = c("model_id", "K")
)
scores
\dontshow{\}) # examplesIf}
}
