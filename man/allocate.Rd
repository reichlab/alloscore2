% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/allocate.R
\name{allocate}
\alias{allocate}
\title{Allocate a budget to minimize expected gpl loss}
\usage{
allocate(
  df = NULL,
  K,
  target_names = NA,
  F = NULL,
  Q = NULL,
  w = 1,
  kappa = 1,
  alpha = 1,
  g = "x",
  dg = NA,
  eps_lam = 1e-04,
  eps_K = 0.01,
  point_mass_window = 0.001
)
}
\arguments{
\item{df}{data frame with one row per target. Columns of \code{df} supply the
other arguments: an argument left empty is filled from a like-named column,
and an argument whose value is a length-1 string naming a column of \code{df} is
replaced by that column, so \code{w = "c"} uses the \code{c} column as weights.}

\item{K}{vector of budgets. Cannot be supplied via \code{df}.}

\item{target_names}{names for the allocation targets, or the name of a column
of \code{df} holding them. Defaults to the row indices.}

\item{F}{list of predictive cdfs, one per target.}

\item{Q}{list of predictive quantile functions, one per target.}

\item{w}{numeric vector of costs per unit of resource allocated to each
target.}

\item{kappa}{scale factor.}

\item{alpha}{normalized loss when the outcome \code{y} exceeds the allocation \code{x}.
Exactly one of \code{alpha} and \code{U} must be supplied.}

\item{g}{a non-decreasing increment function, supplied either as a function
or as a string in the variable \code{x} such as \code{"log(x)"}.}

\item{dg}{derivative of the increment function \code{g}. If \code{NA}, \code{g} is
differentiated symbolically with \code{\link[stats:D]{stats::D()}}.}

\item{eps_lam}{relative tolerance for terminating the bisection on lambda.}

\item{eps_K}{relative tolerance on the budget, below which no post-processing
is attempted.}

\item{point_mass_window}{distance by which search intervals are widened in
order to catch point masses in the \code{F}s, intended or otherwise.}
}
\value{
A tibble of class \code{allocated}, with one row per value of \code{K} and
columns
\describe{
\item{K}{the budget.}
\item{x}{list of named allocation vectors.}
\item{xs}{list of data frames holding the allocation at each iteration.}
\item{qs_OK}{whether the unconstrained (quantile) solution already
satisfied the budget.}
\item{lam, lamL, lamU}{final Lagrange multiplier and its bracketing
interval, with \verb{*_seq} columns holding the full iteration history.}
\item{post_processed}{whether plateau post-processing was applied.}
\item{xdf}{list of per-target tibbles holding the allocation and a
scoring function.}
}
The \code{gpl_df}, \code{w} and \code{target_col_name} attributes carry the loss
functions, weights and target column name.
}
\description{
Solves
\deqn{\min_x \sum_i E_{Y_i \sim F_i} L_i(x_i, Y_i)
      \quad\text{subject to}\quad \sum_i w_i x_i \le K,\ x_i \ge 0,}
where each \eqn{L_i} is a generalized piecewise linear loss (see
\code{\link[=gpl_loss_fun]{gpl_loss_fun()}}). The problem is separable and convex, so at the optimum the
marginal expected benefit \eqn{\Lambda_i} (see \code{\link[=meb_gpl_df]{meb_gpl_df()}}) is equal
across all targets receiving a positive allocation. \code{allocate()} bisects on
that common value \eqn{\lambda}, inverting \eqn{\Lambda_i} for each target at
each step. All values of \code{K} are solved simultaneously, sharing root-finding
work wherever they currently agree on \eqn{\lambda}.
}
\examples{
fc <- add_pdqr_funs(
  tibble::tibble(
    target_names = c("a", "b", "c"),
    dist = "norm",
    mean = c(5, 8, 12),
    sd = c(1, 2, 3)
  ),
  types = c("p", "q")
)
a <- allocate(fc, K = c(10, 20), alpha = 0.9)
a$xdf[[1]]

}
