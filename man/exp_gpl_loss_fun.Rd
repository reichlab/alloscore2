% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpl_loss.R
\name{exp_gpl_loss_fun}
\alias{exp_gpl_loss_fun}
\title{Create an expected gpl loss function}
\usage{
exp_gpl_loss_fun(
  dg = function(u) 1,
  F,
  kappa = 1,
  alpha = NA,
  O = NA,
  U = NA,
  offset = 0
)
}
\arguments{
\item{dg}{derivative of the increment function \code{g}.}

\item{F}{predictive cdf of the outcome.}

\item{kappa}{scale factor.}

\item{alpha}{normalized loss when the outcome \code{y} exceeds the allocation \code{x}.
Exactly one of \code{alpha} and \code{U} must be supplied.}

\item{O}{cost incurred when the allocation \code{x} exceeds the outcome \code{y};
equals \code{kappa * (1 - alpha)}.}

\item{U}{cost incurred when the outcome \code{y} exceeds the allocation \code{x};
equals \code{kappa * alpha}.}

\item{offset}{a constant added to the expected loss. Unlike
\code{\link[=gpl_loss_fun]{gpl_loss_fun()}}, a function-valued \code{offset} is not supported here, since
its expectation would itself require integration.}
}
\value{
A function of an allocation \code{x} giving the expected loss with
respect to the distribution \code{F}.
}
\description{
This is the per-target contribution to the objective minimized by
\code{\link[=allocate]{allocate()}}.
}
\examples{
Z <- exp_gpl_loss_fun(F = pnorm, alpha = 0.5)
Z(0)

}
