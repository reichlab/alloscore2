% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_out.R
\name{as_alloscore_df}
\alias{as_alloscore_df}
\title{Convert hubverse model output into forecasts alloscore2 can allocate against}
\usage{
as_alloscore_df(model_out_tbl, oracle_output = NULL, target_cols)
}
\arguments{
\item{model_out_tbl}{a hubverse model output table containing a single
\code{output_type}, which must be \code{"quantile"}.}

\item{oracle_output}{hubverse oracle output, holding the observed values in
an \code{oracle_value} column. Optional when only allocating.}

\item{target_cols}{character vector naming the task ID columns whose
combinations enumerate the targets that share a budget, for example
\code{"location"}.}
}
\value{
A tibble with one row per allocation unit, the allocation unit
columns, and a \code{forecasts} list column. Each element of \code{forecasts} is a
tibble with one row per target holding the \code{target_cols}, a
\code{target_names} key, the predictive quantile levels \code{ps} and values \code{qs},
the cdf \code{F} and quantile function \code{Q}, and -- when \code{oracle_output} was
supplied -- the observed outcome \code{y}.
}
\description{
Groups a hubverse model output table into allocation problems, turns each
target's predictive quantiles into a cdf and quantile function using
\code{\link[distfromq:make_p_fn]{distfromq::make_p_fn()}} and \code{\link[distfromq:make_q_fn]{distfromq::make_q_fn()}}, and attaches the
observed outcomes from \code{oracle_output}.
}
\details{
An allocation problem is a set of targets that share one budget.
\code{target_cols} names the task ID columns whose combinations enumerate those
targets; every other task ID column, together with \code{model_id}, defines the
\emph{allocation unit}, and one allocation problem is solved per combination of
those. This mirrors the way a hubverse \code{compound_taskid_set} distinguishes
task IDs that vary within a group from those held constant.
}
\examples{
\dontshow{if (requireNamespace("hubExamples", quietly = TRUE)) withAutoprint(\{ # examplesIf}
mot <- dplyr::filter(hubExamples::forecast_outputs, output_type == "quantile")
adf <- as_alloscore_df(
  mot,
  oracle_output = hubExamples::forecast_oracle_output,
  target_cols = "location"
)
adf
adf$forecasts[[1]]
\dontshow{\}) # examplesIf}
}
