% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpl_df.R
\name{new_gpl_df}
\alias{new_gpl_df}
\title{Create a data frame of gpl loss functions and their parameters}
\usage{
new_gpl_df(
  N = NULL,
  target_names = NA,
  g = "x",
  dg = NA,
  kappa = 1,
  alpha = 1,
  O = NA,
  U = NA,
  offset = 0
)
}
\arguments{
\item{N}{number of targets. If \code{NULL}, inferred from the longest argument.}

\item{target_names}{names of the targets, one per row.}

\item{g}{a non-decreasing increment function, supplied either as a function
or as a string in the variable \code{x} such as \code{"log(x)"}.}

\item{dg}{derivative of \code{g}. Defaults to \code{NA}, in which case it is derived
symbolically from \code{g} when needed.}

\item{kappa}{scale factor.}

\item{alpha}{normalized loss when the outcome \code{y} exceeds the allocation \code{x}.
Exactly one of \code{alpha} and \code{U} must be supplied.}

\item{O}{cost incurred when the allocation \code{x} exceeds the outcome \code{y};
equals \code{kappa * (1 - alpha)}.}

\item{U}{cost incurred when the outcome \code{y} exceeds the allocation \code{x};
equals \code{kappa * alpha}.}

\item{offset}{a constant, or a function of \code{y}, added to the loss. The
default of \code{0} gives a loss with \code{L(x, x) = 0}.}
}
\value{
A tibble of class \code{gpl_df} with one row per target, columns for each
loss parameter, and a \code{gpl_loss_fun} list column of loss functions.
}
\description{
Recycles scalar arguments to length \code{N} and builds one gpl loss function per
target.
}
\examples{
new_gpl_df(N = 3, alpha = c(0.5, 0.7, 0.9), target_names = c("a", "b", "c"))

}
