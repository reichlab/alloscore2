% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/oracle.R
\name{oracle_allocate_direct}
\alias{oracle_allocate_direct}
\title{Find the oracle allocation directly}
\usage{
oracle_allocate_direct(y, K, w)
}
\arguments{
\item{y}{observed outcomes, one per target.}

\item{K}{vector of budgets. Cannot be supplied via \code{df}.}

\item{w}{numeric vector of costs per unit of resource allocated to each
target.}
}
\value{
A numeric vector of allocations.
}
\description{
A closed form for the oracle's allocation, avoiding the machinery of
\code{\link[=allocate]{allocate()}}. If the observed outcomes are affordable the oracle allocates
exactly those; otherwise it scales them down proportionally. Assumes all
targets share the same gpl parameters and that \code{g} is the identity.
}
\examples{
oracle_allocate_direct(y = c(3, 5), K = 4, w = 1)
oracle_allocate_direct(y = c(3, 5), K = 100, w = 1)

}
