% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alloscore.R
\name{alloscore}
\alias{alloscore}
\alias{alloscore.default}
\alias{alloscore.allocated}
\alias{alloscore.slim}
\title{Score allocations against realized outcomes}
\usage{
alloscore(df = NULL, ...)

\method{alloscore}{default}(
  df = NULL,
  K,
  target_names = NA,
  y,
  F = NULL,
  Q = NULL,
  w = 1,
  kappa = 1,
  alpha = 1,
  g = "x",
  dg = NA,
  eps_K = 0.01,
  eps_lam = 1e-04,
  against_oracle = TRUE,
  slim = FALSE,
  ...
)

\method{alloscore}{allocated}(df, y, against_oracle = TRUE, ...)

\method{alloscore}{slim}(df, ys, against_oracle = TRUE, ...)
}
\arguments{
\item{df}{a data frame of forecasts, an \code{allocated} object (as returned by
\code{\link[=allocate]{allocate()}}), or a \code{slim} object (as returned by \code{\link[=slim]{slim()}}). The method
dispatched on determines which further arguments apply.}

\item{...}{arguments passed to methods.}

\item{K}{vector of budgets. Cannot be supplied via \code{df}.}

\item{target_names}{names for the allocation targets, or the name of a column
of \code{df} holding them. Defaults to the row indices.}

\item{y}{numeric vector of observed outcomes, one per target.}

\item{F}{list of predictive cdfs, one per target.}

\item{Q}{list of predictive quantile functions, one per target.}

\item{w}{numeric vector of costs per unit of resource allocated to each
target.}

\item{kappa}{scale factor.}

\item{alpha}{normalized loss when the outcome \code{y} exceeds the allocation \code{x}.
Exactly one of \code{alpha} and \code{U} must be supplied.}

\item{g}{a non-decreasing increment function, supplied either as a function
or as a string in the variable \code{x} such as \code{"log(x)"}.}

\item{dg}{derivative of the increment function \code{g}. If \code{NA}, \code{g} is
differentiated symbolically with \code{\link[stats:D]{stats::D()}}.}

\item{eps_K}{relative tolerance on the budget, below which no post-processing
is attempted.}

\item{eps_lam}{relative tolerance for terminating the bisection on lambda.}

\item{against_oracle}{logical; if \code{TRUE}, components and scores relative to
the oracle allocation are included.}

\item{slim}{logical; if \code{TRUE}, drop the heavy list columns before scoring.
See \code{\link[=slim]{slim()}}.}

\item{ys}{a list of named outcome vectors, whose names must match the target
names of \code{df}.}
}
\value{
A tibble of the form returned by \code{\link[=allocate]{allocate()}}, with the class
\code{scored} prepended and additional columns
\describe{
\item{components_raw}{the realized gpl loss at each target (inside \code{xdf}).}
\item{score_raw}{the sum of \code{components_raw}.}
\item{components_oracle}{the oracle's loss at each target (inside \code{xdf}).}
\item{score_oracle}{the sum of \code{components_oracle}.}
\item{components}{\code{components_raw - components_oracle} (inside \code{xdf}).}
\item{score}{\code{score_raw - score_oracle}.}
\item{ytot}{the total weighted outcome, \code{sum(w * y)}.}
}
}
\description{
Allocates a budget under a set of forecasts and then scores the realized gpl
loss of that allocation, optionally relative to the loss of an oracle that
knew the outcomes in advance. The oracle-relative score is non-negative and
is zero for a perfect forecast.
}
\section{Methods (by class)}{
\itemize{
\item \code{alloscore(default)}: Allocates and then scores, for forecasts supplied as a
data frame or as individual arguments.

\item \code{alloscore(allocated)}: Scores an allocation that has already been computed.

\item \code{alloscore(slim)}: Scores a slim allocation against many outcome vectors,
for Monte Carlo work. Uses \code{\link[=oracle_alloscore_direct]{oracle_alloscore_direct()}} rather than a full
oracle allocation.

}}
\examples{
fc <- add_pdqr_funs(
  tibble::tibble(
    target_names = c("a", "b", "c"),
    dist = "norm",
    mean = c(5, 8, 12),
    sd = c(1, 2, 3)
  ),
  types = c("p", "q")
)
s <- alloscore(fc, y = c(4, 9, 11), K = c(10, 20))
s[, c("K", "score", "score_raw", "score_oracle")]

}
