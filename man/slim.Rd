% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slim.R
\name{slim}
\alias{slim}
\title{Drop the heavy list columns of an allocated data frame}
\usage{
slim(
  adf,
  xdf_action = c("default", "unnest", "nest"),
  id_cols = NULL,
  rm_score_fun_if_scored = TRUE,
  rm_score_fun_if_not_scored = FALSE
)
}
\arguments{
\item{adf}{an \code{allocated} object, as returned by \code{\link[=allocate]{allocate()}}.}

\item{xdf_action}{whether to unnest the \code{xdf} column. The default unnests an
unscored allocation and leaves a scored one nested.}

\item{id_cols}{additional columns to keep, such as a model name or origin
time.}

\item{rm_score_fun_if_scored}{drop the per-target scoring closure from a
scored data frame, where it is usually no longer needed.}

\item{rm_score_fun_if_not_scored}{drop the per-target scoring closure from an
unscored data frame. Defaults to \code{FALSE}, since it is what
\code{\link[=alloscore.slim]{alloscore.slim()}} scores with.}
}
\value{
A tibble of class \code{slim}, carrying the \code{gpl_df}, \code{w} and
\code{target_col_name} attributes of \code{adf}.
}
\description{
An \code{allocated} object carries the full lambda-iteration history and a closure
per target, which is far more than is needed to score many outcome vectors
against the same allocation. \code{slim()} keeps the budgets, the allocations and
the scores.
}
\examples{
fc <- add_pdqr_funs(
  tibble::tibble(target_names = c("a", "b"), dist = "norm", mean = c(5, 8), sd = 1),
  types = c("p", "q")
)
slim(allocate(fc, K = c(10, 20)))

}
