% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pdqr.R
\name{pdqr_factory}
\alias{pdqr_factory}
\title{Build a p/d/q/r function with parameters fixed}
\usage{
pdqr_factory(..., dist, type, trans = NULL, trans_inv = NULL, transpars = NULL)
}
\arguments{
\item{...}{parameter name-value pairs to be fixed in the returned function.
Parameters not accepted by the target distribution function are ignored, so
entire rows of a forecast data frame may be passed in.}

\item{dist}{distribution root name, such as \code{"norm"} or \code{"beta"}, or
\code{"distfromq"} to interpolate a distribution through predictive quantiles.}

\item{type}{one of \code{"p"}, \code{"d"}, \code{"q"} or \code{"r"}.}

\item{trans}{function of an outcome \code{x} and parameters, pre-composed with the
\code{p} and \code{d} functions. Use this for distributions whose support must be
rescaled, such as a beta distribution on \verb{[x_min, x_max]}.}

\item{trans_inv}{inverse of \code{trans}, post-composed with the \code{q} and \code{r}
functions.}

\item{transpars}{parameters for \code{trans} and \code{trans_inv} that are not columns
of \code{df}.}
}
\value{
A function of one argument: a cdf (\code{"p"}), density (\code{"d"}), quantile
function (\code{"q"}) or random generator (\code{"r"}).
}
\description{
Build a p/d/q/r function with parameters fixed
}
\examples{
F <- pdqr_factory(mean = 5, sd = 2, dist = "norm", type = "p")
F(5)

# a distribution interpolated through predictive quantiles
Q <- pdqr_factory(
  ps = c(0.25, 0.5, 0.75), qs = c(4, 5, 6),
  dist = "distfromq", type = "q"
)
Q(0.5)

}
