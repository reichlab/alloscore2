% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/oracle.R
\name{oracle_allocate}
\alias{oracle_allocate}
\title{Allocate as an oracle that knows the observed outcomes}
\usage{
oracle_allocate(gpl_df, y, K, w = 1, ...)
}
\arguments{
\item{gpl_df}{a \code{gpl_df} (see \code{\link[=new_gpl_df]{new_gpl_df()}}) or an \code{allocated} object, from
which the loss parameters and weights are taken.}

\item{y}{observed outcomes, one per target.}

\item{K}{vector of budgets. Cannot be supplied via \code{df}.}

\item{w}{numeric vector of costs per unit of resource allocated to each
target.}

\item{...}{further arguments passed to \code{\link[=allocate]{allocate()}}.}
}
\value{
An \code{allocated} tibble; see \code{\link[=allocate]{allocate()}}.
}
\description{
Runs \code{\link[=allocate]{allocate()}} with degenerate predictive distributions that place all of
their probability on the observed outcomes, giving the allocation against
which a forecaster's allocation is scored. Because those distributions are
point masses, the objective is piecewise constant and
\code{\link[=post_process]{post_process()}} is always needed.
}
\examples{
fc <- add_pdqr_funs(
  tibble::tibble(target_names = c("a", "b"), dist = "norm", mean = c(5, 8), sd = 1),
  types = c("p", "q")
)
a <- allocate(fc, K = 10)
oracle_allocate(a, y = c(4, 9), K = 10)$x

}
