% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pdqr.R
\name{add_pdqr_funs}
\alias{add_pdqr_funs}
\title{Add list columns of p/d/q/r functions to a forecast data frame}
\usage{
add_pdqr_funs(
  df,
  dist = df$dist,
  trans = NULL,
  trans_inv = NULL,
  transpars = NULL,
  types = c("p", "d", "q", "r"),
  fnames = c(p = "F", d = "f", q = "Q", r = "r")
)
}
\arguments{
\item{df}{data frame with one row per target. It must have a column for each
parameter required by the distribution functions named in a \code{dist} column
or in the \code{dist} argument.}

\item{dist}{character vector of length 1 or \code{nrow(df)} giving distribution
root names (or \code{"distfromq"}); required if \code{df} has no \code{dist} column.}

\item{trans}{function of an outcome \code{x} and parameters, pre-composed with the
\code{p} and \code{d} functions. Use this for distributions whose support must be
rescaled, such as a beta distribution on \verb{[x_min, x_max]}.}

\item{trans_inv}{inverse of \code{trans}, post-composed with the \code{q} and \code{r}
functions.}

\item{transpars}{parameters for \code{trans} and \code{trans_inv} that are not columns
of \code{df}.}

\item{types}{which of \code{"p"}, \code{"d"}, \code{"q"}, \code{"r"} to add.}

\item{fnames}{names to give the added list columns.}
}
\value{
A tibble with the requested list columns added.
}
\description{
Given a data frame with one row per target, a \code{dist} column (or a \code{dist}
argument) naming a distribution, and columns for that distribution's
parameters, adds list columns holding the corresponding cdf, density,
quantile function and/or random generator. \code{\link[=allocate]{allocate()}} needs only the cdf
(\code{"p"}) and quantile function (\code{"q"}).
}
\examples{
fc <- add_pdqr_funs(
  tibble::tibble(
    target_names = c("a", "b"),
    dist = "norm",
    mean = c(5, 8),
    sd = c(1, 2)
  ),
  types = c("p", "q")
)
fc$F[[1]](5)

}
